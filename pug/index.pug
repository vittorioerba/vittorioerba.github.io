include includes/mixins.pug
include includes/data.pug

+page_skeleton

    // ---------------------------------------------------------------------------------------------------------------------
    // About me
    // ---------------------------------------------------------------------------------------------------------------------
    
    .fl.w-100.pb4.tc
        +summary
            | I'm a Ph.D. student in Statistical Mechanics at the University of Milan, where I work under the supervision of 
            +link('http://pcteserver.mi.infn.it/~caraccio/') Professor Sergio Caracciolo.
            br
            br
            | I'm interested in how Euclidean and non-Euclidean metric structures affect the collective behaviour of large numbers of interacting degrees of freedom, both in low and high dimension and in presence of disorder.
        .pt3
            .flex.flex-wrap.justify-around
                +float_box
                    h1.f3.black.b
                        | Education
                    hr.mw5.bb.bw1.b--black-10
                    table.f5.w-100.mw8.center(cellspacing='0')
                        thead
                            tr
                                td.pv3.pr3.bb.b--black-20.tr(colspan=4)
                        
                        tbody.lh-copy
                            +table_separator
                            each item in cv_education 
                                tr
                                    +table_line 
                                        .tc.tr-m.tr-l #{item.date}
                                    +table_line 
                                        .tc #{item.what}
                                    if item.url
                                        +table_line
                                            .tc.tl-m.tl-l
                                                +link(item.url) #{item.where}
                                    else
                                        +table_line 
                                            .tc.tl-m.tl-l #{item.where}
                                +table_separator

                            tr.dn.dt-row-m.dt-row-l
                                td.pv3.pr3.bb.b--black-20.tr(colspan='3')
                                    hr.mw5.bb.b--dotted.bw1.b--black-10

                            +table_separator
                            each item in cv_schools
                                tr
                                    +table_line 
                                        .tc.tr-m.tr-l #{item.date}
                                    +table_line 
                                        .tc #{item.what}
                                    if item.url
                                        +table_line
                                            .tc.tl-m.tl-l 
                                                +link(item.url) #{item.where}
                                    else
                                        +table_line 
                                            .tc.tl-m.tl-l #{item.where}
                                +table_separator
                            // each item in cv_schools
                            //     tr
                            //         td.pv3.pr3.bb.b--black-20.tr #{item.date}
                            //         td.pv3.pr3.bb.b--black-20.tc #{item.what}
                            //         if item.url
                            //             td.pv3.pr3.bb.b--black-20.tl 
                            //                 +link(item.url) #{item.where}
                            //         else
                            //             td.pv3.pr3.bb.b--black-20.tl #{item.where}
                +float_box
                    h1.f3.black.b
                        | Work
                    hr.mw5.bb.bw1.b--black-10
                    table.f5.w-100.mw8.center(cellspacing='0')
                        thead
                            tr
                                td.pv3.pr3.bb.b--black-20.tr(colspan=4)
                        tbody.lh-copy
                            +table_separator
                            each item in cv_work
                                tr
                                    +table_line 
                                        .tc.tr-m.tr-l #{item.date}
                                    +table_line 
                                        .tc #{item.what}
                                    if item.url
                                        +table_line
                                            .tc.tl-m.tl-l
                                                +link(item.url) #{item.where}
                                    else
                                        +table_line 
                                            .tc.tl-m.tl-l #{item.where}
                                +table_separator
                            // each item in cv_work
                            //     tr
                            //         td.pv3.pr3.bb.b--black-20.tr #{item.date}
                            //         td.pv3.pr3.bb.b--black-20.tc #{item.what}
                            //         if item.url
                            //             td.pv3.pr3.bb.b--black-20.tl 
                            //                 +link(item.url) #{item.where}
                            //         else
                            //             td.pv3.pr3.bb.b--black-20.tl #{item.where}
                            //
                    
                    p.center.mt5.f4
                        | See my full CV 
                        +link("./data/VittorioErba_CV.pdf") here 

            +float_clear

        // ---------------------------------------------------------------------------------------------------------------------
        // Research projects 
        // ---------------------------------------------------------------------------------------------------------------------

        +section_start
            +section_title
                | Active Research Projects

            each item in projects.filter(function(e){return e.active})
                -   var page_url = "./"+item.page+".html"
                +banner_element
                    +banner_text
                        b.f3 #{item.name}
                        br
                        small.f4.black-70 #{item.collab}
                        p.lh-copy.center.f4.black-70 !{item.description}

                        .flex.flex-wrap.justify-around
                            +float_box
                                -   if ((publications.filter(function(e){return e.project == item.project;})).length >= 1 )
                                    small.f4.b Relevant publications
                                    br
                                    table.f5.w-100.mw8.center(cellspacing='0')
                                        thead
                                            tr
                                                th.fw6.bb.b--black-20.tr.pb3.pr3.bg-white
                                        tbody.lh-copy
                                            each item2 in publications.filter(function(e){return e.project == item.project;})
                                                tr
                                                    td
                                                        .pv3.pr3.bb.b--black-20
                                                            | #{item2.authors}
                                                            br
                                                            b #{item2.title}
                                                            br
                                                            | #{item2.journal}
                                                            br
                                                            +link(item2.url) #{item2.doi}
                                -   else
                                    | Not published yet, stay tuned!

                            +float_box
                                -   if ((materials.filter(function(e){return e.project == item.project;})).length >= 1 )
                                    small.f4.b Relevant materials
                                    br
                                    table.f5.w-100.mw8.center(cellspacing='0')
                                        thead
                                            tr
                                                th.fw6.bb.b--black-20.tr.pb3.pr3.bg-white(colspan=4)
                                        tbody.lh-copy
                                            +table_separator
                                            each item2 in materials.filter(function(e){return e.project == item.project;})
                                                -   var url=item2.url
                                                tr
                                                    +table_line
                                                        .tc.tr-m.tr-l !{item2.date}
                                                    - if (item2.where=="")
                                                        +table_line_2 
                                                            .tc !{item2.name}
                                                    - else
                                                        +table_line 
                                                            .tc.tr-m.tr-l !{item2.where}
                                                        +table_line 
                                                            .tc.tl-m.tl-l !{item2.name}
                                                    +table_line
                                                        .tc.tl-m.tl-l
                                                            +link(url)
                                                                b !{item2.desc}
                                                +table_separator
                                -   else
                                    | I haven't produced any material yet!

                        +float_clear

        // ---------------------------------------------------------------------------------------------------------------------
        // Materials
        // ---------------------------------------------------------------------------------------------------------------------
        +section_start
            .flex.flex-wrap.justify-around
                +float_box
                    +section_title All Publications
                    table.f5.w-100.mw8.center(cellspacing='0')
                        thead
                            tr
                                th.fw6.bb.b--black-20.tr.pb3.pr3.bg-white
                        tbody.lh-copy
                            each item in publications
                                tr
                                    td
                                        .pv3.pr3.bb.b--black-20
                                            | #{item.authors}
                                            br
                                            b #{item.title}
                                            br
                                            | #{item.journal}
                                            br
                                            +link(item.url) #{item.doi}

                +float_box
                    +section_title All Materials
                    table.f5.w-100.mw8.center(cellspacing='0')
                        thead
                            tr
                                th.fw6.bb.b--black-20.tr.pb3.pr3.bg-white(colspan=4)
                        tbody.lh-copy
                            +table_separator
                            each item in materials
                                -   var file_url=item.url
                                tr
                                    +table_line
                                        .tc.tr-m.tr-l !{item.date}
                                    - if (item.where=="")
                                        +table_line_2 
                                            .tc !{item.name}
                                    - else
                                        +table_line 
                                            .tc.tr-m.tr-l !{item.where}
                                        +table_line 
                                            .tc.tl-m.tl-l !{item.name}
                                    +table_line
                                        .tc.tl-m.tl-l
                                            +link(file_url)
                                                b !{item.desc}
                                +table_separator

            +float_clear


        // ---------------------------------------------------------------------------------------------------------------------
        // non active Research projects 
        // ---------------------------------------------------------------------------------------------------------------------

        +section_start
            +section_title
                | Old Research Projects

            each item in projects.filter(function(e){return !e.active})
                -   var page_url = "./"+item.page+".html"
                +banner_element
                    +banner_text
                        b.f3 #{item.name}
                        br
                        small.f4.black-70 #{item.collab}
                        p.lh-copy.center.f4.black-70 !{item.description}

                        .flex.flex-wrap.justify-around
                            +float_box
                                -   if ((publications.filter(function(e){return e.project == item.project;})).length >= 1 )
                                    small.f4.b Relevant publications
                                    br
                                    table.f5.w-100.mw8.center(cellspacing='0')
                                        thead
                                            tr
                                                th.fw6.bb.b--black-20.tr.pb3.pr3.bg-white
                                        tbody.lh-copy
                                            each item2 in publications.filter(function(e){return e.project == item.project;})
                                                tr
                                                    td
                                                        .pv3.pr3.bb.b--black-20
                                                            | #{item2.authors}
                                                            br
                                                            b #{item2.title}
                                                            br
                                                            | #{item2.journal}
                                                            br
                                                            +link(item2.url) #{item2.doi}
                                -   else
                                    | Not published yet, stay tuned!

                            +float_box
                                -   if ((materials.filter(function(e){return e.project == item.project;})).length >= 1 )
                                    small.f4.b Relevant materials
                                    br
                                    table.f5.w-100.mw8.center(cellspacing='0')
                                        thead
                                            tr
                                                th.fw6.bb.b--black-20.tr.pb3.pr3.bg-white(colspan=4)
                                        tbody.lh-copy
                                            +table_separator
                                            each item2 in materials.filter(function(e){return e.project == item.project;})
                                                -   var file_url="./data/"+item2.file
                                                tr
                                                    +table_line
                                                        .tc.tr-m.tr-l !{item2.date}
                                                    +table_line 
                                                        .tc.tr-m.tr-l !{item2.where}
                                                    +table_line 
                                                        .tc.tl-m.tl-l !{item2.name}
                                                    +table_line
                                                        .tc.tl-m.tl-l
                                                            +link(file_url)
                                                                b !{item2.desc}
                                                +table_separator
                                -   else
                                    | I haven't produced any material yet!

                        +float_clear
